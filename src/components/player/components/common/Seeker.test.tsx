import React from 'react';
import { render } from '@testing-library/react';
import Seeker from './Seeker';

test('renders the seeker component', () => {
    // check the correct width of seek component
    // const seeker = render(<Seeker percentage={40} color="blue" />);
    // expect(seeker).toBeInTheDocument();
});
